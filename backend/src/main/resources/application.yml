server:
  port: 8080
  address: 0.0.0.0

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/dictionary?createDatabaseIfNotExist=true
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: hosszu-asdassd-afds-ffffff-asfsdf-tujtu3e2-2343zrezdre3